# Secrets (local environment files are never committed)
.env.local

# CDK Build Artifacts
cdk/cdk.out/
cdk/node_modules/

# CLI Internals
.cli/*

# Ephemeral Staging
.cache/*

# Per-user CLI target (contains AWS account id) — use aws-targets.json.example as a template
aws-targets.json
